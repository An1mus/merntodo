{
    "title": "Authentication",
    "description": "Properties required for a user to log in",
    "type": "object",
    "properties": {
        "login": {
            "type": "string",
            "description": "login, which can be email, or username"
        },
        "password": {
            "type": "string",
            "description": "password"
        }
    },
    "required": ["login", "password"]
}
