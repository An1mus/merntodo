name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Run Playwright tests
        run: npx playwright test
