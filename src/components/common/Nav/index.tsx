import React from 'react';
import {NavLink} from 'react-router-dom';

const Nav = () => {
    return (
        <>
            <NavLink to={'/'}>List</NavLink>
        </>
    );
};

export default Nav;
