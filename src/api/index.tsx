import db from './indexDB';

// A job that is running in background to check the App state and update the current lists,
// as well as update the stats

export const backgroundJob = () => {

}

export {db}
